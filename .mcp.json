{
  "mcpServers": {
    "repo-relationship-graph": {
      "command": "sh",
      "args": [
        "-lc",
        "uv run --project \"${CODE_GRAPH_PLUGIN_ROOT:-$HOME/plugins/repo-relationship-graph}\" python -m repo_graph.mcp_server --config codegraph.config.toml"
      ]
    }
  }
}
